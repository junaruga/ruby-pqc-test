# RubyGems HTTP Server Test

## Usage

1. Set up the RubyGems server environment.

   ```
   $ script/setup.sh
   ```

2. Run the RubyGems HTTP server.

   ```
   $ script/run_server.sh
   ```

3. Test the client in another terminal.

   ```
   $ script/test_client.sh
   ```
